import { HeroSystemDiagram } from "harborline-systems";

// The animated hero "operating layer" diagram: six business systems (Website,
// Leads, CRM, Data, Reporting, Automation) connected into one central hub, with
// flowing gold connectors and a caption. Self-sufficient.
export function Default() {
  return <HeroSystemDiagram />;
}
